# Changelog

## [Unreleased]

### Added
- RSpec / Capybara テストファイルから Spekta アノテーションを自動生成
- `feature` / `describe` / `context` / `scenario` / `it` の構造認識
- Capybara DSL（visit, fill_in, click_on, select, expect）のステップ変換
- `page_from` 設定: `manual`（デフォルト）/ `filename`（ファイル名からページ名を自動生成）
